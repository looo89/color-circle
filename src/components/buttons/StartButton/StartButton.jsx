import React from "react";
import classes from "./StartButton.module.css"


const StartButton=(props)=>{
    return <div className={classes.myButton}>
        Start
    </div>
}

export default StartButton;